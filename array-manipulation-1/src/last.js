/* exported last */
// Pseudocode:/
// Define a new function with the name last with one parameter (array)
// Grab the last element of the array and assign it to a new variable lastElement
// return the expression of the array at the index of the last element

function last(array) {
  var lastElement = array.length - 1;
  return array[lastElement];
}
