/* exported head */

// Pseudo Code://
// Define a new function with the name head with one parameter (array)
// Assign the first index of an element to a new variable
// Return the result of the expression

function head(array) {
  var first = array[0];
  return first;
}
