/* exported setValue */
// Pseudocode
// Define a new function named setValue with 3 parameters (object, key, value)
// Assign value to the new key property of the object array with bracket notation

function setValue(object, key, value) {
  object[key] = value;
}
