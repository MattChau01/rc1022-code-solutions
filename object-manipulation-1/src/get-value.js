/* exported getValue */
// Pseudocode
// Define a new function named getValue with two parameters (object, key)
// Use bracket notation to find the value of the key property of the object and assign it to a new variable value
// Return value at the end of the function

function getValue(object, key) {
  var value = object[key];
  return value;
}
