/* exported getFirstChar */
// Pseudocode:
// Define a new function name getFirstChar with one parameter (string)
// Find the letter of the string at the 0 index and assign this to a new variable firstLetter
// Return the value of the variable firstLetter

function getFirstChar(string) {
  var firstLetter = string[0];
  return firstLetter;
}
